// alert("Hello JS");

function pageInit() {
	
	//定义变量
	var a = 6;
	var b = 3;
	// alert(a+b);
	
	//使用数组
	var arr = [2, 6, "asdas", 5.1, 3.14];
	/*alert("数组的长度是："+arr.length+",此时数组的第一个元素是："+arr[0]);
	
	alert("删除第一个元素");
	arr.shift();
	alert("此时第一个元素的值是："+arr[0]+",数组的长度是："+arr.length);
	
	alert("删除最后一个元素");
	arr.pop();
	alert("此时数组的长度是："+arr.length);
	
	alert("在数组最后添加一项");
	arr.push("Hello JS");
	alert("数组的最后一项是："+arr[arr.length-1]+",此时数组的长度是："+arr.length);*/
	
	// arr.splice(1,1);
	// arr.splice(1,0,"Hello JS Array");
	// alert(arr[1]);
	// alert(arr[2]);
	
	//HashMap,HashTable
	var obj = {index:8,str:"JavaScript"};
	alert(obj.str);
}
